        <div class="p-2 flex-1 w-full + (task.done? 'line-through text-gray-400':'')">
            {task.title}
          </div>


             <div
            class={
              'p-2 flex-1 w-full' + 
              (task.done ? ' line-through text-gray-400' : '')
            }>
            {task.title}
          </div>

          (task.done ? 'line-through text-gray-400' : '')
          (task.done ? ' line-through text-gray-400' : '')